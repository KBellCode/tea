class Tree{
    Node root;


    public Node addToTree(Node current, String payload, char result){
        if (current == null){
            return new Node(payload);
        }

        if (result == 'Y'){
            
        }
        
        //premise for going left or rught in the tree. as it is string
        //
    }
    
}